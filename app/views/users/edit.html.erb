<div class="ui raised very padded text center aligned container segment">
<%= form_for(@user) do |f| %>
	<%= f.label "Name" %> 
	<%= f.text_field :name %>
	<%= f.label "Age" %>
	<%= f.text_field :age %>
	<%= f.submit %>
<% end %>

</div>