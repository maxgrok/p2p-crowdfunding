<div class="ui raised very padded text center aligned container segment">
	<h1>Sign Up</h1>
	<%= form_for @user do |f| %>
		<%= f.label "Name" %> 
		<%= f.text_field :name %><br>
		<%= f.label "Age" %>
		<%= f.text_field :age %><br>
		<%= f.submit %>
	<% end %>
</div>